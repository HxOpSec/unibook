name: unibook
description: "UniBook university library app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  firebase_core: ^3.15.1
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  provider: ^6.1.5
  syncfusion_flutter_pdfviewer: ^30.2.7
  file_picker: ^10.3.2
  dio: ^5.9.0
  cached_network_image: ^3.4.1
  shared_preferences: ^2.5.3
  connectivity_plus: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
